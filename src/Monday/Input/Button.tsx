export const Button = () => {
    return (
    const  onClickButtonHandler = () => {
        props.addMessage(title);
        setTitle('')
    }

        <>
            <button onClick={onClickButtonHandler}>+</button>
        </>
    )
}